#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

int tcase,t;
cin>>tcase;
while(tcase--){
    cin>>t;
    vector<int>v;
    int maxm=0;
    int minm=0;
    for(int i=0;i<t;i++){
        cin>>a;
        int sum=0
        int a=false;
        int b=false;
        v.push_back(a);
        if(v[i]>maxm){
            maxm=v[i];
        }
              if(v[i]<minm){
            mimm=v[i];
        }

    }

    for(int i=0;i<t;i++){
        if(v[i]==maxm){
            sum=+i+1;
            a=true;
        }
        if(v[i]==minm){
            sum=
        }


    }



}






}





